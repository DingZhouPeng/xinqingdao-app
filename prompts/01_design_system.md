请重点打磨前端 UI。阅读 `docs/03_UI_DESIGN_SYSTEM.md`，优化 `src/styles/theme.css`、`src/styles/app.css`、`src/components/IslandScene.tsx`、`src/pages/HomePage.tsx`。

要求：

- 视觉像移动端 APP，不像普通网页。
- 375px 宽度优先。
- 首页有心晴岛、天气、云朵、灯塔、主 CTA、今日补给。
- 卡片有层次、渐变、柔和阴影。
- 不引入新的 UI 库。
- 修改后运行 `npm run build`。
