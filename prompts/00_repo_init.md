请作为本项目的工程负责人，先阅读 `AGENTS.md`、`README.md`、`PLANS.md` 和 `docs/` 下所有文件。不要急着大改代码。先输出：

1. 当前项目结构是否合理。
2. 哪些文件最影响 MVP。
3. 你准备按什么顺序实现。
4. 需要运行哪些测试命令。

随后执行 `npm run typecheck`、`npm run test`、`npm run build`，修复错误。
